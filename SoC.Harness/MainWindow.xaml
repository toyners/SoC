<Window x:Class="SoC.Harness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoC.Harness"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
  <DockPanel>
    <Grid DockPanel.Dock="Left">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>

      <local:PlayerControl x:Name="TopLeftPlayer" />
      <local:PlayerControl x:Name="BottomLeftPlayer" Grid.Row="1" />
    </Grid>

    <Grid DockPanel.Dock="Right">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition />
      </Grid.RowDefinitions>

      <local:PlayerControl x:Name="TopRightPlayer"/>
      <local:PlayerControl x:Name="BottomRightPlayer" Grid.Row="1" />
    </Grid>

    <local:PlayAreaControl x:Name="PlayArea" Grid.Column="1" />
  </DockPanel>
</Window>
